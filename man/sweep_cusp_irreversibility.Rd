% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{sweep_cusp_irreversibility}
\alias{sweep_cusp_irreversibility}
\title{Sweep the cusp parameter \code{a} and compute loop area at each value}
\usage{
sweep_cusp_irreversibility(
  a_grid,
  control_values = seq(-2, 2, length.out = 100),
  seed = 42L,
  initial_state = 0
)
}
\arguments{
\item{a_grid}{Numeric vector. Values of the cusp parameter \code{a} to sweep.}

\item{control_values}{Numeric vector. The control parameter path (\code{b}
values). Default \code{seq(-2, 2, length.out = 100)}.}

\item{seed}{Integer. Seed for reproducibility. Default 42.}

\item{initial_state}{Numeric. Starting state for each forward sweep.
Default 0.}
}
\value{
List (A6):
\item{values}{List: \code{sweep} (data frame: a, loop_area, has_hysteresis,
max_difference), \code{peak_loop_area}, \code{peak_a}, \code{bifurcation_a}}
\item{metadata}{List: \code{n}, \code{a_grid}, \code{control_range}, \code{seed},
\code{initial_state}, \code{method}, \code{converged}}
}
\description{
For each \code{a} in \code{a_grid}, creates a cusp equilibrium function
(\code{\link[=make_cusp_equilibrium_fn]{make_cusp_equilibrium_fn()}}), runs \code{\link[=hysteresis_loop_area]{hysteresis_loop_area()}}, and records
the loop area. This shows irreversibility emerging at the bifurcation
(\code{a = 0}): for \code{a >= 0} (no bifurcation) the loop area is 0; for \code{a < 0}
(cusp region) the loop area rises as \verb{|a|} increases (the cusp region
widens, producing a larger hysteresis loop).
}
\section{Theoretical Context}{


The framework Prediction: irreversibility emerges discontinuously at the bifurcation.
For \code{a >= 0} the system has a single equilibrium (no bifurcation, fully
reversible, loop area = 0). For \code{a < 0} the system has a cusp (two stable
equilibria); the loop area grows as \verb{|a|} increases because the cusp
region widens. This makes irreversibility \emph{quantitative} and shows exactly
where it emerges.

Competitor: gradual reversibility predicts loop area = 0 for all \code{a} (no
bifurcation ever).
}

\examples{
result <- sweep_cusp_irreversibility(
  a_grid = seq(1, -2, by = -0.25),
  control_values = seq(-2, 2, length.out = 100)
)
result$values$peak_a # most negative a (largest loop area)
}
