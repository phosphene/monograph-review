% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{generate_transfer_data}
\alias{generate_transfer_data}
\title{Generate synthetic transfer data (internal)}
\usage{
generate_transfer_data(
  n_species = 8,
  n_gene_categories = 6,
  n_birds = 10,
  dep_coef = 0.8,
  para_coef = -0.5,
  noise_sd = 0.05,
  seed = 42L
)
}
\arguments{
\item{n_species}{Integer. Number of plant species. Default 8.}

\item{n_gene_categories}{Integer. Number of gene categories. Default 6.}

\item{n_birds}{Integer. Number of bird structures. Default 10.}

\item{dep_coef}{Numeric. True dependency coefficient (> 0, valence). Default 0.8.}

\item{para_coef}{Numeric. True parasitism coefficient (< 0). Default -0.5.}

\item{noise_sd}{Numeric. SD of retention noise on the plant matrix. Default 0.05.}

\item{seed}{Integer. RNG seed. Default 42.}
}
\value{
List: plant_data (data.frame), bird_data (data.frame).
}
\description{
Generates a synthetic plant retention matrix (species x gene_category)
and matching bird data with VARYING parasitism scores, both from the
same true model (known dep_coef > 0, para_coef < 0). The plant matrix
has controllable noise; the bird data is noise-free (it is the
"ground truth" ordering to be predicted).
}
\details{
Plant and bird use SEPARATE seed streams (plant = seed, bird = seed +
1000) so that plant noise does not shift the bird RNG state — the bird
data is identical across noise levels, isolating the effect of plant
noise on the transfer.
}
\keyword{internal}
