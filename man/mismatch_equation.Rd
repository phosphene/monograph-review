% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cultural_ode_models.R
\name{mismatch_equation}
\alias{mismatch_equation}
\title{Mismatch equation: M(t) = ρ_eq(B(t)) - ρ(t)}
\usage{
mismatch_equation(t, rho_eq0, r_B, k1)
}
\arguments{
\item{t}{Numeric vector. Time.}

\item{rho_eq0}{Numeric. Initial attractor value.}

\item{r_B}{Numeric. Cultural acceleration rate (ε·β).}

\item{k1}{Numeric. Fast biological relaxation rate.}
}
\description{
In the exponential cultural regime (β > 1, r_B = ε·β):
M(t) = ρ_eq(B0) · (exp(r_B·t) - exp(k1·t))
}
\details{
When r_B > k1: M(t) > 0 and growing (attractor ahead of relaxation)
When r_B < k1: M(t) < 0 (relaxation has overshot — not physical,
means mismatch is negligible, organism tracks environment)
When r_B = k1: M(t) = 0 (matched)

Use abs(M) for magnitude, or check sign for regime.
}
