% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cusp_catastrophe.R
\name{make_cusp_equilibrium_fn}
\alias{make_cusp_equilibrium_fn}
\title{Create a pure branch-following equilibrium function}
\usage{
make_cusp_equilibrium_fn(a = -1)
}
\arguments{
\item{a}{Numeric. First control parameter (splitting factor). For \code{a < 0}
the system is in the cusp region (three real roots for some \code{b});
for \code{a >= 0} there is a single real root for all \code{b}.}
}
\value{
Function \verb{(control_b, prev_state) -> numeric} (nearest stable root).
}
\description{
Returns a PURE function \verb{(control_b, prev_state) -> next_state} that solves
the cusp equilibrium x^3 + a*x + b = 0 and returns the real root nearest
\code{prev_state} (branch-following). For single-root regions \code{prev_state} is
ignored. This function holds NO internal state — the branch-following
state is threaded by \code{\link[=cusp_hysteresis_check]{cusp_hysteresis_check()}}, which makes the path-
dependence contract explicit (previously this was a stateful closure using
\verb{<<-}, which hid the detection logic in the simulacrum; see math-review
Issue 4, resolved in Phase 3).
}
\section{Theoretical Context}{


Valence Prediction: irreversibility — the cusp catastrophe's fold bifurcation
produces path-dependent equilibria. For \code{a < 0}, the system has two stable
equilibria separated by an unstable one; crossing the bifurcation set
(\verb{4a^3 + 27b^2 = 0}) forces a sudden jump to the other branch. The branch-
following contract makes this visible: a forward sweep (increasing \code{b})
stays on one branch, while the reverse sweep (decreasing \code{b}) stays on
the other — producing hysteresis.

Competitor: gradual reversibility (standard quantitative genetics predicts
smooth recovery when selection pressure is removed — no bifurcation, no
hysteresis).
}

\examples{
eq_fn <- make_cusp_equilibrium_fn(a = -1)
eq_fn(0.3, 1.0)  # root nearest 1.0 (upper branch)
}
