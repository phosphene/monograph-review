% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/economics.R
\name{stochastic_cdi}
\alias{stochastic_cdi}
\title{Stochastic CDI first-passage simulation}
\usage{
stochastic_cdi(
  mu0,
  sigma0,
  cdi_init = 0.01,
  dt = 0.01,
  n_steps = 1000L,
  threshold = 0.8,
  seed = 42L
)
}
\arguments{
\item{mu0}{Numeric. Logistic drift-rate constant (positive).}

\item{sigma0}{Numeric. Base volatility (>= 0).}

\item{cdi_init}{Numeric. Initial CDI in \link{0, 1}. Default 0.01.}

\item{dt}{Numeric. Time step. Default 0.01.}

\item{n_steps}{Integer. Number of time steps. Default 1000.}

\item{threshold}{Numeric. First-passage CDI threshold in (0, 1]. Default 0.8.}

\item{seed}{Integer. Seed for reproducibility (A2: injectable).}
}
\value{
List (A6: proof object):
\item{values}{Named numeric: final_cdi, max_cdi, first_passage
(NA if not reached), n_steps}
\item{metadata}{List: seed, n (n_steps + 1), mu0, sigma0, dt, threshold,
path (full numeric CDI vector), time (numeric vector), reached}
}
\description{
Simulates a single stochastic Commitment/Disintegration Index (CDI) path
under the framework logistic-drift, commitment-damped-volatility SDE and returns
the first-passage time to a threshold. Adapted from Paper 2's
\code{simulate_cdi_path} as a pure, seedable function.
}
\section{Theoretical Context}{


the framework's prediction: commitment (CDI) is autocatalytic under logistic drift and
reaches irreversibility (threshold) with probability 1, while volatility is
damped as commitment grows (\code{sigma0 * (1 - CDI)}). First-passage times are
the economic analog of time-to-irreversibility in the monograph.

Competitor: a constant-drift / constant-volatility random walk (Merton-style
baseline) predicts the same mean first-passage time but with hazard that is
constant over time, not increasing with CDI. The logistic-drift + damped
volatility structure DOES distinguish the framework from the constant baseline.

What supports the framework: first-passage times that are realistic and hazard that
increases with CDI. What refutes the framework: paths that wander without converging
to the threshold.
}

\examples{
\dontrun{
stochastic_cdi(
  mu0 = 0.5, sigma0 = 0.2, cdi_init = 0.01,
  dt = 0.01, n_steps = 1000, threshold = 0.8, seed = 42
)
}
}
