% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_loaders.R
\name{load_retention_matrix}
\alias{load_retention_matrix}
\title{Load Orobanchaceae retention matrix
Loads the 8-species × 6-gene-category plastid-gene retention matrix with
parasitism scores and integration-depth (dependency) scores. This is the
empirical data for the formal model (the author's original 8×6 matrix,
correctly flattened gene-major — see Remark R7).}
\usage{
load_retention_matrix()
}
\value{
List with data (data frame) and metadata.
}
\description{
Load Orobanchaceae retention matrix
Loads the 8-species × 6-gene-category plastid-gene retention matrix with
parasitism scores and integration-depth (dependency) scores. This is the
empirical data for the formal model (the author's original 8×6 matrix,
correctly flattened gene-major — see Remark R7).
}
\section{Theoretical Context}{

The retention matrix is the empirical basis for \code{empirical_formal_model()}:
Valence predicts dep > 0 (deeper integration → more retention) and para < 0
(deeper parasitism → less retention). The competitor (random loss)
predicts no dep effect. The author's original additive GLM produced the
wrong sign due to a data-flattening bug (\code{as.vector(t(retention))} is
species-major; \code{rep(dep_scores, each=8)} is gene-major). This dataset
uses the corrected flattening (\code{as.vector(retention)}, gene-major).
}

