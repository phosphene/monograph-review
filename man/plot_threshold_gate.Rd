% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{plot_threshold_gate}
\alias{plot_threshold_gate}
\title{Visualize the threshold gate: θ vs biphasicity}
\usage{
plot_threshold_gate(sweep_result)
}
\arguments{
\item{sweep_result}{List. A \code{\link[=sweep_threshold]{sweep_threshold()}} result.}
}
\value{
A ggplot2 object.
}
\description{
Plots the protection threshold θ (x-axis) against \code{threshold_biphasicity}
(y-axis) from a \code{\link[=sweep_threshold]{sweep_threshold()}} result. The gate "opens" (biphasicity
→ 1) when θ separates protected from unprotected traits, and "closes"
(biphasicity → 0) at the edges. Vertical dashed lines mark each trait's
depth (where the gate composition changes).
}
\section{Theoretical Context}{


The threshold gate is the heart of the framework's biphasic prediction. Seeing it
respond to θ builds the intuition that the biphasic signal is \emph{the gate},
not the displacement ratio (math-review Issue 3, resolved). The shaded
region is the "gate open" zone — the parameter range where the framework's biphasic
prediction is active.
}

\examples{
\dontrun{
result <- sweep_threshold(depths = c(0, 1, 2, 3, 5), theta_grid = seq(0, 6, 0.5))
plot_threshold_gate(result)
}
}
