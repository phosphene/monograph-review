% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_step.R
\name{fit_step}
\alias{fit_step}
\title{Fit a Heaviside step function to (theta, rho) data}
\usage{
fit_step(theta, rho)
}
\arguments{
\item{theta}{Numeric vector. Provision depth (θ).}

\item{rho}{Numeric vector. Retention correlation (ρ).}
}
\value{
List with elements:
\describe{
\item{step}{List: theta_star, rho_sat, rss, aic}
\item{sigmoid}{List: k, x0, rho_sat, rss, aic}
\item{best_model}{Character: "step" or "sigmoid"}
\item{delta_aic}{Numeric: sigmoid AIC minus step AIC (positive = step wins)}
}
}
\description{
Searches for the breakpoint θ* that minimizes residual sum of squares
between a two-level step function and the data. Estimates ρ_sat as the
mean of post-breakpoint values. Compares the step model against the best
sigmoid (logistic function) on AIC.
}
\details{
This is the core fitter for the original VI formula ρ(θ) = ρ_sat · H(θ − θ*).
The successor relaxation formula — dρ/dt = −k₁(ρ − ρ₁) − k₂(ρ − ρ₂) —
is implemented in \code{fit_biexp.R} (bi-exponential time-series fitter) and
\code{relaxation_model.R} (ODE simulation). Both fitters are preserved:
the step function for cross-sectional (theta, rho) data, the bi-exponential
for temporal (t, rho) data.

It is extracted from \code{test-simulacrum-step-recovery.R} to satisfy the
separation-of-concerns requirement (design-spec.md): Layer 2 fitters
live in \verb{R/}, Layer 3 tests live in \verb{tests/testthat/}.
}
\section{DFT Axioms}{

\itemize{
\item A1 (pure-io-separation): pure function, no I/O
\item A2 (determinism): deterministic given inputs
\item A6 (check-result): returns structured list with model diagnostics
}
}

\examples{
theta <- seq(-1, 1, length.out = 20)
rho <- ifelse(theta < 0, 0.05, 0.35) + rnorm(20, 0, 0.01)
fit_step(theta, rho)
}
