% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{hysteresis_loop_area}
\alias{hysteresis_loop_area}
\title{Compute the hysteresis loop area}
\usage{
hysteresis_loop_area(
  control_values,
  equilibrium_fn,
  seed = 42L,
  initial_state = 0
)
}
\arguments{
\item{control_values}{Numeric vector. The control parameter path (e.g., a
sequence of \code{b} values from low to high).}

\item{equilibrium_fn}{Function. Pure \verb{(control_value, prev_state) -> state}.
Typically from \code{\link[=make_cusp_equilibrium_fn]{make_cusp_equilibrium_fn()}}.}

\item{seed}{Integer. Seed for reproducibility. Default 42.}

\item{initial_state}{Numeric. Starting state for the forward sweep. Default 0.}
}
\value{
List (A6):
\item{values}{Named: \code{loop_area}, \code{max_difference}, \code{has_hysteresis},
\code{n_control_values}}
\item{metadata}{List: \code{seed}, \code{n}, \code{control_range}, \code{initial_state},
\code{method}, \code{converged}}
}
\description{
Runs \code{\link[=cusp_hysteresis_check]{cusp_hysteresis_check()}} and computes the area enclosed between the
forward and reverse equilibrium paths using the trapezoid rule. Zero area
means no hysteresis (the system is reversible); large area means strong
irreversibility (the forward and reverse paths enclose a loop).
}
\details{
This is the \emph{quantitative} irreversibility metric — \code{\link[=cusp_hysteresis_check]{cusp_hysteresis_check()}}
returns a boolean (\code{has_hysteresis}) and a max pointwise difference; this
function returns the full loop area, which is a continuous measure of how
irreversible the system is.
}
\section{Theoretical Context}{


Valence Prediction: irreversibility — the forward path (increasing commitment)
differs from the reverse path (decreasing commitment). The loop area
quantifies \emph{how much} they differ. A system with no bifurcation (e.g.,
\code{a >= 0} in the cusp) has loop area = 0 (fully reversible). A system deep
in the cusp region (\verb{a << 0}) has a large loop area (strongly irreversible).

Competitor: gradual reversibility predicts loop area = 0 always (no
bifurcation, smooth recovery).
}

\examples{
eq_fn <- make_cusp_equilibrium_fn(a = -1)
result <- hysteresis_loop_area(seq(-2, 2, length.out = 100), eq_fn)
result$values$loop_area  # > 0 (cusp region: irreversible)
}
