% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{plot_irreversibility_sweep}
\alias{plot_irreversibility_sweep}
\title{Visualize the irreversibility sweep: \code{a} vs loop area}
\usage{
plot_irreversibility_sweep(sweep_result)
}
\arguments{
\item{sweep_result}{List. A \code{\link[=sweep_cusp_irreversibility]{sweep_cusp_irreversibility()}} result.}
}
\value{
A ggplot2 object.
}
\description{
Plots the cusp parameter \code{a} (x-axis) against the hysteresis loop area
(y-axis) from a \code{\link[=sweep_cusp_irreversibility]{sweep_cusp_irreversibility()}} result. The loop area is 0
for \code{a >= 0} (no bifurcation, reversible) and rises for \code{a < 0} (cusp
region, irreversible). The cusp region is shaded; the bifurcation point
(\code{a = 0}) is marked with a dashed line.
}
\section{Theoretical Context}{


Irreversibility is the framework's sharpest departure from gradual reversibility.
This visualization shows that irreversibility is \emph{quantitative} (loop
area, not just a boolean) and that it emerges at the bifurcation (\code{a = 0}).
The shaded region is where the framework's irreversibility prediction is active.
}

\examples{
\dontrun{
result <- sweep_cusp_irreversibility(a_grid = seq(1, -2, by = -0.25))
plot_irreversibility_sweep(result)
}
}
