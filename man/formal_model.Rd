% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formal_model.R
\name{formal_model}
\alias{formal_model}
\title{Formal dynamical model of threshold-gated capacity reallocation}
\description{
The framework formal model: dC_i/dt = -λ × M(t) × C_i × I(d_i < θ)
}
\details{
NOTE: the C_i factor (current retention) is essential — without it the ODE
is linear (dC/dt = -λM) whose solution C(T) = 1 - λ∫M dt goes negative;
with it the ODE is exponential (dC/dt = -λMC) whose solution
C(T) = exp(-λ∫M dt) stays in \link{0, 1}, matching retention_at_time().
where C_i = retention probability of trait i, M(t) = decaying niche-demand
mismatch, d_i = integration depth, θ = protection threshold, λ = shedding rate.

This module implements the \emph{cross-sectional} formal model: for a given set
of integration depths, how does retention depend on the protection threshold?
The \emph{temporal} counterpart — the relaxation formula
dρ/dt = −k₁(ρ − ρ₁) − k₂(ρ − ρ₂) — is implemented in \code{relaxation_model.R}.
Both models converge on the same biphasic prediction.

This module implements:
\itemize{
\item Numerical integration of the formal model (Euler method)
\item Analytical closed-form solution
\item Convergence proof between numerical and analytical solutions
\item Equilibrium predictions and phase transition analysis
}
}
\section{Theoretical Context}{


the framework's prediction: biphasic kinetics — fast Phase 1 (unprotected traits shed
rapidly) followed by slow Phase 2 (protected traits resist loss).

Competitors:
\itemize{
\item Constant rate (relaxed selection, Lahti 2009): predicts linear reduction
\item Accelerating (Muller's ratchet): predicts accelerating reduction
\item The biphasic (logistic/saturation) shape is unique to the framework's threshold-gated model
}
}

