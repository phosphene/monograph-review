% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_registry.R
\name{register_test}
\alias{register_test}
\title{Register an empirical test}
\usage{
register_test(
  name,
  fn,
  data_required = character(0),
  discriminating = FALSE,
  expected_fields = character(0),
  class = "vi_test_result",
  statistic_key = NULL,
  vi_prediction = ""
)
}
\arguments{
\item{name}{Character. Unique test name.}

\item{fn}{Function. Implements the test; accepts \code{...}.}

\item{data_required}{Character vector. Names of the data arguments the test
needs (e.g. \code{"data"}, \code{c("plant_data", "bird_data")}). Use
\code{character(0)} for tests that need no external data.}

\item{discriminating}{Logical. Whether the test distinguishes the framework from
competitors (as opposed to only being consistent with the framework).}

\item{expected_fields}{Character vector. Value fields the test returns.}

\item{class}{Character. S3 result class to wrap the output in
(default \code{"vi_test_result"}).}

\item{statistic_key}{Character. Optional name of the raw value field to use
as the canonical statistic (otherwise auto-detected).}

\item{vi_prediction}{Character. Plain-language the framework's prediction statement.}
}
\value{
Invisibly returns the registry entry.
}
\description{
Adds (or replaces) a test in the registry. The test function must return a
list with \code{values} (named list of numeric metrics) and \code{metadata} (named
list of provenance) — the A6 proof-object shape used by the Empirical layer.
}
\examples{
\dontrun{
register_test(
  name = "my_test", fn = my_test,
  data_required = "data", discriminating = TRUE,
  expected_fields = c("rho", "p_value"), class = "vi_ordering_result"
)
}
}
