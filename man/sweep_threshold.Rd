% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{sweep_threshold}
\alias{sweep_threshold}
\title{Sweep the protection threshold θ across a grid}
\usage{
sweep_threshold(
  depths,
  theta_grid,
  lambda = 0.15,
  m0 = 10,
  alpha = 0.05,
  time = 100
)
}
\arguments{
\item{depths}{Numeric vector. The dependency architecture (integration
depths of a hypothetical organism's traits).}

\item{theta_grid}{Numeric vector. θ values to sweep.}

\item{lambda}{Numeric. Shedding rate. Default 0.15.}

\item{m0}{Numeric. Initial mismatch. Default 10.}

\item{alpha}{Numeric. Mismatch decay rate. Default 0.05.}

\item{time}{Numeric. Total integration time. Default 100.}
}
\value{
List (A6):
\item{values}{List: \code{sweep} (data frame: theta, threshold_biphasicity,
n_protected, n_unprotected, mean_retention), \code{peak_biphasicity},
\code{peak_theta}, \code{results} (full per-θ result list)}
\item{metadata}{List: n, n_traits, depths, params, method, seed, converged}
}
\description{
For each θ in \code{theta_grid}, runs \code{\link[=threshold_model]{threshold_model()}} and records the
biphasic signal (\code{threshold_biphasicity}), the protected/unprotected
counts, and the mean retention. This makes the threshold gate \emph{visible} as
a control parameter: the gate opens (biphasicity → 1) when θ separates
protected from unprotected traits, and closes (biphasicity → 0) when θ is
below the minimum depth (all protected) or above the maximum depth (all
unprotected).
}
\section{Theoretical Context}{


Valence Prediction: biphasic kinetics — the threshold gate separates protected
traits (d ≥ θ, retention = 1.0) from unprotected traits (d < θ, retention
→ 0). The biphasic signal IS the gate (math-review Issue 3, resolved).

This sweep shows the gate as a control parameter: shifting θ changes which
traits are protected, and the biphasic signal responds discontinuously at
each trait's depth. This makes the R6 method-misspecification visceral: a
cross-sectional regression across organisms with \emph{different} θ would see
noise, not a logistic — which is exactly why T3 fails on real data.

Competitors: constant rate (no gate, no biphasicity), accelerating (no
gate). The biphasic gate is unique to valence's threshold-gated model.
}

\examples{
result <- sweep_threshold(
  depths = c(0, 1, 2, 3, 5),
  theta_grid = seq(0, 6, by = 0.5)
)
result$values$peak_theta  # θ where the gate is maximally open
}
