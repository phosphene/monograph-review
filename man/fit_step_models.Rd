% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/economics_formula.R
\name{fit_step_models}
\alias{fit_step_models}
\title{Heaviside step model fit}
\usage{
fit_step_models(theta, rho)
}
\arguments{
\item{theta}{Numeric vector. Provision depth (θ).}

\item{rho}{Numeric vector. Retention correlation (ρ).}
}
\value{
List: \code{step} (list: theta_star, rho_sat, rss, aic),
\code{sigmoid} (list: k, x0, rss, aic), \code{logistic} (list: k, x0, rss, aic),
\code{best_model} (character), \code{delta_aic_step_vs_sigmoid} (numeric)
}
\description{
Fits ρ(θ) = ρ_sat · H(θ − θ*) to (theta, rho) data by searching
for the breakpoint θ* that minimizes residual sum of squares, then
estimating ρ_sat as the mean of post-breakpoint values.
}
\details{
Returns the fitted step model alongside sigmoid and logistic competitors
for AIC comparison.
}
\keyword{internal}
