% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{sweep_endogenous_k}
\alias{sweep_endogenous_k}
\title{Sweep the endogenous-K feedback parameter}
\usage{
sweep_endogenous_k(
  feedback_grid,
  n_steps = 20,
  innovation_rate = 0.3,
  capacity = 30,
  seed = 42L
)
}
\arguments{
\item{feedback_grid}{Numeric vector. Feedback values in \link{0, 1} to sweep.}

\item{n_steps}{Integer. Time series length. Default 20.}

\item{innovation_rate}{Numeric. Base rate. Default 0.3.}

\item{capacity}{Numeric. Carrying capacity. Default 30.}

\item{seed}{Integer. Default 42.}
}
\value{
List (A6):
\item{values}{List: \code{sweep} (data frame: feedback, dd_slope, dd_sign),
\code{bifurcation_feedback} (theoretical = 2/3), \code{measured_bifurcation_feedback}}
\item{metadata}{List: \code{n}, \code{feedback_grid}, \code{params}, \code{method}, \code{seed},
\code{converged}}
}
\description{
Sweeps the feedback parameter (cultural complexity / endogenous-K strength)
across a grid and returns the diversity-dependence sign at each level. Shows
the bifurcation from negative DD (niche-filling, feedback < 2/3) to positive
DD (Homo inversion, feedback > 2/3). The theoretical bifurcation is at
feedback = 2/3; the measured bifurcation (from the linear regression in
\code{\link[=diversity_dependence_sign]{diversity_dependence_sign()}}) is approximate but close.
}
\section{Theoretical Context}{


Valence Prediction: the Homo inversion is a bifurcation — there is a threshold
in cultural complexity (feedback strength) beyond which diversity-
dependence flips from negative (niche-filling) to positive (autocatalytic).
This sweep makes the bifurcation explorable: below the threshold, the
system is niche-filling (competitor); above it, the system is autocatalytic. The threshold is the endogenous-K bifurcation (Review Item 3).
}

\section{Risk note}{


See \code{\link[=diversity_dependence_contrast]{diversity_dependence_contrast()}} — the feedback formula corrects a bug
in the original plan. The theoretical bifurcation is at feedback = 2/3
(where the analytical DD slope = 0). The \emph{measured} bifurcation (where the
linear regression slope crosses zero) is approximate because the regression
fits a linear approximation to a nonlinear per-capita-rate function; the
sign is reliable but the exact crossing point may differ slightly from 2/3.
}

\examples{
result <- sweep_endogenous_k(feedback_grid = seq(0, 1, by = 0.1))
result$values$bifurcation_feedback  # theoretical = 2/3
}
